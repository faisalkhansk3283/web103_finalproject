# Database connection string (Render will provide this)
DATABASE_URL=postgresql://user:password@host:5432/walletwatch

# Set to "true" for production (Render)
DATABASE_SSL=true

# Server port (Render will set this automatically)
PORT=10000

# Environment
NODE_ENV=production
